Run Vale against this project's documentation and report the findings.

Execute:

```bash
vale src/content/docs/
```

List every finding grouped by file. For each finding, show the line number, severity, rule name, and Vale's message. Where the fix is clear, suggest the corrected text inline. Summarize the total finding count by severity at the end.

$ARGUMENTS
